/**
 * SmartInstallBanner — disabled.
 * The product is treated as a standalone app; we don't prompt
 * users to install it as a PWA from inside the app shell.
 */
export function SmartInstallBanner() {
  return null;
}

export default SmartInstallBanner;
