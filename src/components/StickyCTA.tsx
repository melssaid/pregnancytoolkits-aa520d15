/**
 * StickyCTA — disabled.
 * The product is treated as a standalone app, so we don't show
 * "Download from Google Play" sticky CTAs on landing screens.
 */
export function StickyCTA() {
  return null;
}

export default StickyCTA;
