/**
 * SmartAppBanner — disabled.
 * The product is treated as a standalone app, not a website,
 * so we never surface "Get on Google Play" web banners.
 */
export function SmartAppBanner() {
  return null;
}

export default SmartAppBanner;
